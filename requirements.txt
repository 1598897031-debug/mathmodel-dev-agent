# MathModel Dev Agent - 依赖清单

# === Core (required) ===
python-docx>=1.1.0      # Word document generation (PaperAgent)
# No mandatory external deps — the pipeline runs with stdlib only in fallback mode.
# Install these for full functionality:

# LLM backends (at least one needed for LLM-powered parsing/strategy/code-gen)
anthropic>=0.30.0       # Claude API
openai>=1.30.0          # OpenAI API

# Document parsing (PDF support)
pypdf>=4.0.0

# Scientific computing (used by generated solver code)
numpy>=1.24.0

# Visualization (used by experiment agent and solver code)
matplotlib>=3.8.0

# === Optional (dev/tools) ===
scipy>=1.11.0           # Used by some solver templates; solver can work without it
pydantic>=2.7.0         # Data validation (not currently used in core)
typer>=0.12.0           # CLI framework (not currently used; argparse used instead)
rich>=13.0.0            # Terminal formatting (not currently used)

# === Dev/test ===
pytest>=7.4.0
pytest-asyncio>=0.21.0
